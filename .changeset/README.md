# Changesets

This folder is used by [Changesets](https://github.com/changesets/changesets).

Run `npm run changeset` after a meaningful change, then `npm run version` and `npm run release` when you are ready to publish.

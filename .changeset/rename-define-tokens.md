---
'define-tokens': minor
---

Rename the package and CLI from `design-token-package` / `design-tokens` to `define-tokens`.

`TokenSheet` is unchanged; import it from `define-tokens/react` or `define-tokens/next`.
